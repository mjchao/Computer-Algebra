package gui.output;

import gui.DisplayListener;
import gui.Shell;

public class OutputDisplayListener extends DisplayListener {

	
	public OutputDisplayListener( Shell shell, OutputDisplay display ) {
		super( shell, display );
	}
}
